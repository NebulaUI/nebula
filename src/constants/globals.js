import { constants } from 'nebula-react'

const BASE_PATH = '/nebula/'
const NAMESPACE = constants.NAMESPACE
const BREAKPOINTS = ['all', ...constants.BREAKPOINTS]
const MAX_BREAKPOINTS = ['all', ...constants.MAX_BREAKPOINTS]

export { BASE_PATH, BREAKPOINTS, MAX_BREAKPOINTS, NAMESPACE }
